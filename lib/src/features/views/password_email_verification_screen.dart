import 'package:flutter/material.dart';

class PasswordEmailVerificationScreen extends StatelessWidget {
  const PasswordEmailVerificationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
